export enum ElementPosition {
  bottom = 'bottom',
  left = 'left',
  right = 'right',
  top = 'top',
}

export enum Env {
  development = 'development',
  production = 'production',
  test = 'test',
}

export enum HTMLContainerId {
  dropdown = 'dropdown',
  menu = 'menu',
  tooltip = 'tooltip',
}

export enum KeyboardKeys {
  '+' = '+',
  '-' = '-',
  '/' = '/',
  alt = 'Alt',
  a = 'KeyA',
  apostrophe = `'`,
  arrowDown = 'ArrowDown',
  arrowLeft = 'ArrowLeft',
  arrowRight = 'ArrowRight',
  arrowUp = 'ArrowUp',
  b = 'KeyB',
  backspace = 'Backspace',
  c = 'KeyC',
  control = 'Control',
  d = 'KeyD',
  delete = 'Delete',
  e = 'KeyE',
  enter = 'Enter',
  escape = 'Escape',
  f = 'KeyF',
  meta = 'Meta',
  q = 'KeyQ',
  r = 'KeyR',
  s = 'KeyS',
  v = 'KeyV',
  shift = 'Shift',
  tab = 'Tab',
  w = 'KeyW',
  z = 'KeyZ',
  none = 'none',
}

export enum MouseButton {
  idle = 0,
  lmb = 1,
  mmb = 2,
  unknown = 3,
  rmb = 4,
}

export enum MousePosition {
  northWest = 'northWest',
  northEast = 'northEast',
  southWest = 'southWest',
  southEast = 'southEast',
}
