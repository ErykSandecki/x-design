export enum BlockHeight {
  small = 50,
  medium = 70,
  large = 100,
}

export enum BlockWidth {
  small = 50,
  medium = 200,
  large = 350,
}

export enum MouseMode {
  comment = 'comment',
  default = 'default',
  menu = 'menu',
  move = 'move',
}
