// others
import { translationNameSpace as parentNameSpace } from '../../../../constants';

export const BLOCKS_PANEL_ID = 'panelComponents';
export const MIN_HEIGHT = 283;

export const translationNameSpace = `${parentNameSpace}.panelComponents`;
