// others
import { translationNameSpace as parentNameSpace } from '../../../../constants';

export const BLOCKS_PANEL_ID = 'blocksPanel';
export const MIN_HEIGHT = 283;

export const translationNameSpace = `${parentNameSpace}.blocksPanel`;
