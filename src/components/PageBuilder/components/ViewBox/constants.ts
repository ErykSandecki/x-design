// others
import { translationNameSpace as parentNameSpace } from '../../constants';

export const OVERLAY_CONTAINER_ID = 'overlayContainer';
export const THROTTLE_WAIT = 20;
export const translationNameSpace = `${parentNameSpace}.viewBox`;
