// types
import { Header } from './enums';

export const BASE_URL = '';

export const HEADERS = {
  [Header.contentType]: 'application/json',
};

export const RESPONSE_DEV_TIMEOUT = 'dev-response-timeout';

export const translationNameSpace = 'response';
